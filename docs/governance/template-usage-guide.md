# Template Usage Guide

Use `templates/skill-template/` when adding a new skill.

## Rules

- copy the template into `skills/<new-skill>/` ;
- replace placeholders before the skill is referenced anywhere else ;
- keep template examples illustrative and non-authoritative ;
- register any new numbered example in the traceability matrix only after it becomes a real example.
