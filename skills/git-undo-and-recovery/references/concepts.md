# Concepts

- `restore` targets file content.
- `reset` moves branch history and may affect index or worktree.
- `revert` adds a new commit that undoes earlier shared work.
- `reflog` is the recovery ledger for recently moved refs.
