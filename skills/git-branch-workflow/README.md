# git-branch-workflow

Creates or normalizes Git working branches from explicit, safe bases.

Primary use cases:

- new feature branch
- fix branch from stabilization
- local branch rename to meet policy
