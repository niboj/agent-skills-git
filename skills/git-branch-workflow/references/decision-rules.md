# Decision Rules

- Use `feature/`, `fix/`, `hotfix/`, or `chore/` prefixes consistently.
- If the base branch is stale, stop or resynchronize before branching.
- Rename locally before first publication when the branch name is non-compliant.
