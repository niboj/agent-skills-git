# Decision Rules

- Confirm whether the repository is in merge or rebase mode before acting.
- Prefer re-generating generated files instead of hand-merging them.
- Record the reasoning for non-obvious conflict choices.
