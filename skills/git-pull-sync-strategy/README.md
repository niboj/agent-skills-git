# git-pull-sync-strategy

Chooses and documents a safe synchronization strategy instead of running an opaque `git pull`.

This skill is the main entry point for merge-vs-rebase decisions during sync.
