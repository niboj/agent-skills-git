# Decision Rules

- Use rebase for unshared local commits when a linear history is desired.
- Use merge when commit identities are already shared or when policy prefers explicit merge commits.
- Stop when the worktree is dirty or remote state is unclear.
