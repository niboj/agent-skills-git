# Decision Rules

- Use `feat`, `fix`, `docs`, `refactor`, `test`, or `chore` consistently.
- Split a commit when reviewers would need different acceptance criteria for parts of the diff.
- Add a body when the why is not obvious from the subject.
