# Decision Rules

- Match protected branches through an explicit configurable list or pattern set.
- Block direct commit, force push, and remote delete on protected branches.
- Permit `--force-with-lease` only on non-protected personal branches after explicit confirmation.
