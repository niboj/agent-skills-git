# Concepts

- Protected branches carry stricter safety rules than personal branches.
- History rewrites and ref deletions are more dangerous on shared refs than local worktree cleanup.
- Branch classification must be configurable instead of hard-coded to one name only.
